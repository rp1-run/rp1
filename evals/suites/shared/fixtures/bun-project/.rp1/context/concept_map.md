# Domain Concepts

**Project**: rp1-eval-project
**Domain**: Utility scripting

## Core Concepts

### Entry Point
Main script at `src/index.ts` that runs when `bun run dev` is invoked.

### Utility Module
Pure functions in `src/utils.ts` providing reusable logic.

### Test Suite
Bun test files validating utility function behavior.
