# {{ namespacedPluginName }} for Gemini CLI

{{ artifact.description }}

This extension was generated by the rp1 build pipeline for Gemini CLI. It packages rp1 slash commands, Agent Skills, sub-agents, and support-matrix metadata from the same source inputs used by the other rp1 harness bundles.

## Commands

{%- if artifact.commands.size > 0 %}
{%- for command in artifact.commands %}
- `/{{ command }}`
{%- endfor %}
{%- else %}
No commands were generated for this plugin.
{%- endif %}

## Skills

{%- if artifact.skills.size > 0 %}
{%- for skill in artifact.skills %}
- `{{ skill }}`
{%- endfor %}
{%- else %}
No skills were generated for this plugin.
{%- endif %}

## Agents

{%- if artifact.agents.size > 0 %}
{%- for agent in artifact.agents %}
- `@{{ agent }}`
{%- endfor %}
{%- else %}
No agents were generated for this plugin.
{%- endif %}

## Support Matrix

The generated `support-matrix.json` file is the product support boundary for Gemini workflows. Unsupported entries are product-owned exceptions with a rationale and user action, not missing generated assets.
