# {{ namespacedPluginName }}

{{ artifact.description }}

Generated by the rp1 build pipeline for GitHub Copilot's native plugin workflow.

## Included Skills

{%- if artifact.skills.size > 0 %}
{%- for skill in artifact.skills %}
- `{{ skill }}`
{%- endfor %}
{%- else %}
None.
{%- endif %}

## Included Agents

{%- if artifact.agents.size > 0 %}
{%- for agent in artifact.agents %}
- `{{ agent }}`
{%- endfor %}
{%- else %}
None.
{%- endif %}

## Build Metadata

- Version: `{{ pluginVersion }}`
- Plugin: `{{ namespacedPluginName }}`
