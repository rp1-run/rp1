{
  "schemaVersion": 1,
  "managedBy": "rp1",
  "plugin": "{{ namespacedPluginName }}",
  "mcpServers": {},
  "guidance": [
    "rp1 workflow state and artifact operations use the local rp1 CLI unless a workflow declares a separate MCP dependency.",
    "When a workflow needs MCP access, treat missing Antigravity MCP configuration as a support-matrix limitation until validation proves a reliable path."
  ]
}
