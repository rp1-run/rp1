Identify which AI coding harness is executing this prompt and assign `CURRENT_HOST` from: `claude-code`, `codex`, `gemini-cli`, `gh-copilot`, `opencode`, `amp`, `unknown`. Default: `{{ platform | harness_name }}`. Use `CURRENT_HOST` for host-specific decisions and as the `--harness` value in all `rp1 agent-tools` commands.
