{
  "platform": {{ artifact.platformJson }},
  "plugins": {
    "base": {{ artifact.baseJson }},
    "dev": {{ artifact.devJson }}
{%- if artifact.utilsJson %},
    "utils": {{ artifact.utilsJson }}
{%- endif %}
  },
  "version": "{{ version }}",
  "buildTimestamp": "{{ buildTimestamp }}"
}
