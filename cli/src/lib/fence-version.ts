/**
 * Declares the latest fence marker version that ships with this CLI build.
 * Updated when stanza content changes, not on every CLI release.
 * The value is a semver string without a "v" prefix.
 */
export const LATEST_FENCE_VERSION = "0.7.12";
